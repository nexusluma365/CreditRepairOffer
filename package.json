{
  "name": "credit-repair-toolkit-offer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node --check netlify/functions/stripe-config.js && node --check netlify/functions/create-payment-intent.js && node --check netlify/functions/download-product.js"
  },
  "dependencies": {
    "stripe": "^16.12.0"
  }
}
